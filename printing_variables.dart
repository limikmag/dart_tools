void main() {
  List<String> shopList = ['pepper', 'orange', 'banana', 'lettuce'];
  int bucketSize = shopList.length;
  print('Our bucket have a size ${bucketSize} and we have inside: \n${shopList}');
}
